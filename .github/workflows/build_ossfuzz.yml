# Build OSSFuzz fuzz targets from source.
name: build_ossfuzz
on:
  push:
    branches: [main]
permissions: read-all
jobs:
  build_ossfuzz:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
        - os: ubuntu-24.04
          compiler: 'gcc'
          configure_options: ''
    steps:
    - name: Install build dependencies
      run: |
        sudo apt -y install git
    - uses: actions/checkout@v6
      with:
        repository: google/oss-fuzz
        path: oss-fuzz
    - name: Build OSSFuzz fuzz targets
      env:
        BRANCH: ${{ github.head_ref || github.ref_name }}
        ORGANIZATION: ${{ github.event.pull_request.head.repo.owner.login }}
      working-directory: oss-fuzz
      run: |
        mkdir -p projects/libmsiecf
        cp projects/libyal/build.sh projects/libmsiecf/
        cp projects/libyal/project.yaml projects/libmsiecf/
        head -n 20 projects/libyal/Dockerfile > projects/libmsiecf/Dockerfile
        echo "RUN git clone --depth 1 https://github.com/${ORGANIZATION:-libyal}/libmsiecf.git -b ${BRANCH:-main} libmsiecf" >> projects/libmsiecf/Dockerfile
        tail -n 3 projects/libyal/Dockerfile >> projects/libmsiecf/Dockerfile
        python3 infra/helper.py build_image --pull libmsiecf
        python3 infra/helper.py build_fuzzers --sanitizer address libmsiecf
        python3 infra/helper.py check_build libmsiecf
